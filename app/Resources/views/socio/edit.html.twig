{% extends 'base.html.twig' %}

{% block body %}
    <h2 class="text-info bg-info">Editar Socio</h2>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Editar registro" class="btn btn-primary btn-xs"/>
    {{ form_end(edit_form) }}

    {{ form_start(delete_form) }}
                <input type="submit" value="Borrar registro" class="btn btn-primary btn-xs">
    {{ form_end(delete_form) }}
    
    
    <a href="{{ path('socio_index') }}" class="btn btn-info btn-xs" role="button" >Volver al listado</a>
        
{% endblock %}
